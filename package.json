{
  "name": "termina-rubiks-cube",
  "version": "1.0.0",
  "description": "A terminal rubiks cube implementation.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc --skipLibCheck",
    "start": "npm run build && node dist/main.js",
    "dev": "ts-node src/main.ts",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/jest": "^29.5.12",
    "@types/node": "^18.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "inquirer": "^8.2.5"
  }
}
